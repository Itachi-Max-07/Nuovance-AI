# Groq API key for the site assistant (/api/chat).
# Server-side only — NEVER prefix with NEXT_PUBLIC_, that would ship the key
# to the browser. Set it in Vercel under Project Settings > Environment
# Variables, and locally in .env.local (gitignored).
# Free tier keys: https://console.groq.com/keys
GROQ_API_KEY=
